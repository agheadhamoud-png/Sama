// =========================================================================
// Fichier JavaScript Principal
// =========================================================================
// Ce fichier est chargé sur toutes les pages du site.
// Il sert à mettre le code JavaScript commun (menu mobile, animations globales, etc.).

document.addEventListener('DOMContentLoaded', () => {
    console.log('SAMA Website Loaded');

    // Note pour le développeur :
    // La logique spécifique à chaque page (chargement des ateliers, envoi du formulaire de contact...)
    // se trouve directement dans les fichiers HTML concernés (formations.html, contact.html, etc.)
    // dans des balises <script> en bas de page.
    // Cela permet de garder chaque page indépendante.
});
